% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Miscellaneous.R
\name{split.data}
\alias{split.data}
\title{Split Vector, Dataframe or sf feature into n groups}
\usage{
\method{split}{data}(x, n)
}
\arguments{
\item{x}{\code{sf} object or \code{vector} or \code{data.frame}. The data to be split.}

\item{n}{\code{integer}. Non-negative value with a length of 1 to indicate the number of groups the data is to be split.}
}
\value{
Return a list with a length of n, where each element contains a partition of the input data.
}
\description{
This function partitions \code{data.frame}, \code{vector}, or \code{sf} feature into n groups
}
\examples{
# Example 1: Split a numeric vector into 3 groups
v = 1:10
split_v = split.data(v, 3)
print(split_v)

# Example 2: Split a data.frame into 4 groups
df = data.frame(a = 1:8, b = letters[1:8])
split_df = split.data(df, 4)
print(lapply(split_df, nrow)) # Each group's size

# Example 3: Split an sf object into 2 groups and plot
if (requireNamespace("sf", quietly = TRUE)) {
  library(sf)
  nc = st_read(system.file("shape/nc.shp", package="sf"), quiet=TRUE)
  split_nc = split.data(nc, 2)
  plot(st_geometry(split_nc[[1]]), col='red')
  plot(st_geometry(split_nc[[2]]), col='blue', add=TRUE)
}
}
