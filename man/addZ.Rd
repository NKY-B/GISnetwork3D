% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DataManagement.R
\name{addZ}
\alias{addZ}
\title{Add Z (Elevation) Values to 2D POINT or LINESTRING Geometries}
\usage{
addZ(x, y = NULL)
}
\arguments{
\item{x}{An \code{sf} object with 2D \code{POINT} or 2D \code{LINESTRING} geometry. The input features to which Z values will be added.}

\item{y}{\code{NULL} or a single-layer \code{RasterLayer} object. If provided, raster values at each point or vertex are used as Z values. If \code{NULL}, Z values are set to 0.}
}
\value{
A 3D \code{sf} object of the same geometry type as \code{x}, with Z values added as the third coordinate.
}
\description{
Adds Z (elevation) values to each point or vertex of an \code{sf} object with 2D \code{POINT} or 2D \code{LINESTRING} geometry, using values extracted from a raster layer. If no raster is provided, Z values are set to 0.
}
\details{
If \code{y} is a \code{RasterLayer}, Z values are extracted using \code{raster::extract()}. Missing values (\code{NA}) in the raster result in \code{NA} Z values in the output, which may cause issues in downstream processing. Ensure the raster covers the extent of \code{x} to avoid \code{NA} values. The output is a 3D \code{sf} object with Z coordinates added to each point or vertex.
}
\examples{
\dontrun{
library(sf)
library(raster)
# Example raster
r = raster::raster(matrix(1:100, 10, 10), xmn=0, xmx=10, ymn=0, ymx=10)
# Example sf point
pt = st_sf(id=1, geometry=st_sfc(st_point(c(5,5))))
# Add Z from raster
pt_z = addZ(pt, r)
# Add Z=0
pt_z0 = addZ(pt)
}
}
