% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/NetworkConstruction.R
\name{nearestNode3d}
\alias{nearestNode3d}
\title{Find the Nearest Network Node for Each 3D Point}
\usage{
nearestNode3d(node_sf, pt)
}
\arguments{
\item{node_sf}{An \code{sf} object with 3D \code{POINT} geometry, typically from \code{\link[GISnetwork3D]{sf_to_net3d}}, containing a column \code{nID}.}

\item{pt}{An \code{sf} object with 3D \code{POINT} geometry, representing the input points for which to find the nearest network node.}
}
\value{
An \code{sf} object with 3D \code{POINT} geometry, identical to \code{pt}, with an additional column \code{nID} specifying the index of the nearest network node for each point.
}
\description{
Identifies the nearest 3D network node for each input 3D point and attaches its index (\code{nID}).
}
\details{
For each 3D point in \code{pt}, the function identifies the closest 3D node in \code{node_sf} based on 3D Euclidean distance and appends the corresponding \code{nID} as a new column.
}
\examples{
\dontrun{
library(sf)

# Create example network nodes
nodes = st_sf(geometry = st_sfc(
  st_point(c(0, 0, 0)),
  st_point(c(1, 1, 1)),
  st_point(c(2, 2, 2))
))
nodes$nID = 1:nrow(nodes)  # Add node IDs

# Create example points to find nearest nodes
pts = st_sf(geometry = st_sfc(
  st_point(c(0.5, 0.5, 0.5)),
  st_point(c(0, 0, 1.5)),
  st_point(c(3, 3, 3))
))

# Find nearest nodes
nearest_pts = nearestNode3d(nodes, pts)
}

}
